services:
  mongodb:
    image: mongo:4.4
    ports:
      - "27017:27017"

  app:
    build:
      context: ..
      dockerfile: Proyecto 2/Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - mongodb
    environment:
      MONGO_URL: mongodb://mongodb:27017/proyecto2_db
